ניר רצה שנוריד קובץ zip

1.2 minikube start
    minikube status

דוגמה לפקודות git:
    git checkout -b started
    git add .
    git commit -m "Preparing files"
    git push --set-upstream origin started
    git checkout dockerfile
    git pull origin started

2.2 docker build -t shalomberman/week11:latest .
    docker push shalomberman/week11:latest
    docker login

4.1 kubectl apply -f pod.yaml
    kubectl apply -f service.yaml

4.2 kubectl get pods
    kubectl get service
    kubectl get all
